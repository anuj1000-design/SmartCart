########################################
# GITIGNORE CONFIGURATION
########################################
# INSTRUCTIONS:
# - ALWAYS PRIVATE: Never commit these files (even on public repo)
# - CURRENTLY PRIVATE: Only for private repo (uncomment for public)
# - NOT IGNORED: Safe to commit (placeholders/sanitized versions)
########################################

# ===== ALWAYS PRIVATE (NEVER COMMIT) =====
# Security & Credentials - Keep these ignored in ALL repos
web/firebase_config.js
.env
key.properties
**/*.keystore
**/*.jks
lib/firebase_options.dart
android/app/google-services.json
.firebaserc
firestore.rules

# Config.js now holds the credentials (keep private)
web/Config.js

# ===== CURRENTLY PRIVATE (UNCOMMENT FOR PUBLIC REPO) =====
# Remove ## to ignore these when making repo public:
releases/*
!releases/BUILD_INFO.txt
## structure.txt

# ===== BUILD ARTIFACTS & TEMPORARY FILES =====
# Standard Flutter/Dart ignores
## build/
## *.apk
gradle-wrapper.jar
.gradle/
/captures/
/local.properties
GeneratedPluginRegistrant.java
.cxx/
/build
pubspec.lock
.dart_tool/
.firebase/
.vscode/.flutter-plugins
.flutter-plugins-dependencies
.packages

# NPM artifacts (temporary, not needed for Flutter project)
node_modules/
package.json
package-lock.json
android/ndk
android/sdk
flutter/

# OS generated files
Thumbs.db
.DS_Store
._*

# IDE files
.idea/
*.swp
*.suo

# Logs
*.log
logs/

# Coverage reports
coverage/

# Temporary files
*.tmp
*.bak
*.temp

# ===== NOT IGNORED (SAFE TO COMMIT) =====
# These files are safe - they contain placeholder data:
# - web/Smart_Admin.html (sanitized admin panel - used by Firebase /admin route)
# - web/Smart_Landing.html (sanitized landing page - used by Firebase default route)
# - lib/firebase_options_example.dart (template with placeholders)
# - android/app/google-services.json.example (template with placeholders)
# - .firebaserc.example (template with placeholders)
# - firestore.rules.example (template with placeholders)
# - pubspec.yaml
# - All Dart source files
# Note: Edit SmartCart.md and README.md directly when going public

# Local Flutter SDK
flutter_sdk/